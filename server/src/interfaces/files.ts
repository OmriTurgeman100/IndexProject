export interface system_file {
  id: number;
  system_id: number;
  title: string;
  filename: string;
  created_at: string;
}

export interface service_file {
  id: number;
  service_id: number;
  title: string;
  filename: string;
  created_at: string;
}
