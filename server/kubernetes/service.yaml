apiVersion: v1
kind: Service
metadata:
  name: index-server
  labels:
    app: index-server
spec:
  selector:
    app: index-server
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP